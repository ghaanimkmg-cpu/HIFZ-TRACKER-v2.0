Place static assets here (icons, logo, images).

This directory is intentionally empty during Phase 1.
Files added here are served statically — reference them in index.html as:
  <img src="assets/your-file.png" />

No build step required.
